#ifndef CONFIG_H
#define CONFIG_H

/** Configuration file
* Here you can change different constants of the simulation */

/** Resolution */
constexpr unsigned int DEFAULT_WIDTH = 300;
constexpr unsigned int DEFAULT_HEIGHT = 300;
constexpr unsigned int DEFAULT_DEPTH = 10;

constexpr auto PLATFORM = 0;

#endif // !CONFIG_H